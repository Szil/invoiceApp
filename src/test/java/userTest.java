/**
 * Created by Gergo on 2014.06.12..
 */
public class userTest {

    /*@Test
    public void test() {

        user e = new user();
        e.setEmail("maxime@example.com");
        e.setName("Maxime Jonas");
        e.setPassword("notSecret");

        EbeanServer server = Ebean.getServer(null);

        server.save(e);
        Assert.assertNotNull(e.getEmail());

        user e2 = server.find(user.class, e.getEmail());
        Assert.assertEquals("notSecret", e2.getPassword());

        user e3 = new user("hobo@example.com", "Hobo Blues", "secret");
        server.save(e3);

        Assert.assertEquals("hobo@example.com", e3.getEmail());

        user e4 = new user("mkyong..2002@gmail.com", "invalidMail", "secret");
        server.save(e4);
        Assert.assertEquals(null, server.find(user.class, e4.getName()));
    }*/
}
