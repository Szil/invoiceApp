package model;

/**
 * Created by Gergo on 2014.06.24..
 */
public class orgTest {
/*
    @Test
    public void orgTest(){
        Organisation testOrg = new Organisation();
        Organisation testOrg2 = new Organisation();

        testOrg = Ebean.find(Organisation.class).where().eq("orgId", 1).findUnique();
        testOrg2 = Ebean.find(Organisation.class).where().eq("taxId", "").findUnique();

        Assert.assertNotNull(testOrg);
        Assert.assertNull(testOrg2);
    }*/
}
